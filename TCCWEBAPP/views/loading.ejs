<!DOCTYPE html>
<html>
<head>
  <title>Loading...</title>
  <script>
    // Redirect to results page after a delay (e.g., 5 seconds)
    setTimeout(function() {
      window.location.href = '/results';
    }, 10); // Adjust the delay as needed
  </script>
  <style>
    /* Center the loading content */
    body {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    /* Circular progress indicator styles */
    .loader {
      border: 4px solid #f3f3f3;
      border-top: 4px solid #3498db;
      border-radius: 50%;
      width: 50px;
      height: 50px;
      animation: spin 2s linear infinite;
    }

    /* Space between loader and text */
    .loader,
    p {
      margin-top: 20px;
    }

    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

  </style>
</head>
<body>
  <!-- Circular progress indicator -->
  <div class="loader"></div>

  <!-- Loading text in the center -->
  <p class="load">Loading... Hang on tight...</p>
</body>
</html>
